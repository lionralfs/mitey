const { expect } = require("chai");
const cmds = require("../lib/cmds");

describe("Commands", () => {
  describe("verify", () => {
    it("should render a text", () => {
      // TODO
    });
  });
});
